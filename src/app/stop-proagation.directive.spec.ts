import { StopProagationDirective } from './stop-proagation.directive';

describe('StopProagationDirective', () => {
  it('should create an instance', () => {
    const directive = new StopProagationDirective();
    expect(directive).toBeTruthy();
  });
});